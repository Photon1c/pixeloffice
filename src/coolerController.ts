export function addUtteranceToHistory(utterance: string): void {
    // Add the utterance to history log
    console.log(`Adding utterance to history: ${utterance}`);
    // Placeholder logic for adding utterance
}