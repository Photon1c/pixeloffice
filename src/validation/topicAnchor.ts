export function checkKeywordOverlap(keyword1: string, keyword2: string): boolean {
    // Check for overlap between keywords
    console.log(`Checking overlap between: ${keyword1} and ${keyword2}`);
    return keyword1 === keyword2; // Placeholder logic
}